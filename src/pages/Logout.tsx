import React from 'react';

export default function Logout() {
  return (
    <div className="min-h-screen flex items-center justify-center">
      <div className="text-center">
        <h2 className="text-lg font-semibold">Página de logout removida</h2>
        <p className="text-sm text-muted-foreground mt-2">Implementação anterior apagada para recriação do sistema de login.</p>
      </div>
    </div>
  );
}
