import React from 'react';

export default function Login() {
  return (
    <div className="min-h-screen flex items-center justify-center p-6">
      <div className="w-full max-w-md bg-white shadow rounded p-8 text-center">
        <h2 className="text-2xl font-semibold mb-4">Entrada removida</h2>
        <p className="text-sm text-muted-foreground mb-6">A autenticação via Google/Supabase foi removida. A persistência sem login será implementada em seguida.</p>
      </div>
    </div>
  );
} 
